<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.jiaozihang.coinz">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_test"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_test_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".MainActivity"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SecondActivity" />
        <activity android:name=".ThirdActivity" />
        <activity android:name=".WalletActivity" />
        <activity android:name=".WalletActivity_crocodile" />
        <activity android:name=".WalletActivity_turtle" />
        <activity android:name=".WalletActivity_frog" />
        <activity android:name=".WalletActivity_lizard" />
        <activity android:name=".Register" />
        <activity android:name=".Bank" />
        <activity android:name=".trade" />
        <activity android:name=".robbery" />
        <activity android:name=".robbery_buff" />
        <activity android:name=".robbery_fail" />
        <activity android:name=".robbery_success" />

        <!--<activity-->
            <!--android:name=".LoginActivity"-->
            <!--android:label="@string/title_activity_login"></activity>-->
    </application>

</manifest>